import { StyleSheet } from 'react-native';

export const buttonStyle = StyleSheet.create({
  default: {
    justifyContent: 'center',
    alignItems: 'center',
    width: 44,
    height: 44,
  },
});
